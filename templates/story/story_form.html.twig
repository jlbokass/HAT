{{ form_start(storyForm, {'attr': {'novalidate': 'novalidate'}}) }}

        {{ form_label(storyForm.title) }}
        {{ form_widget(storyForm.title) }}
        {{ form_errors(storyForm.title) }}

        {{ form_label(storyForm.content) }}
        {{ form_widget(storyForm.content) }}
        {{ form_errors(storyForm.content) }}

        {{ form_row(storyForm.themes) }}

        {{ form_row(storyForm.image) }}