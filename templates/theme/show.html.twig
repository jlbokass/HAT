{% extends 'base.html.twig' %}

{% block title %}
Voir le theme
{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>{{ theme.name }}</h1>
        <p>{{ theme.description }}</p>

        <p><a href="{{ path('home_theme') }}">Retour à la liste des themes</a></p>
    </div>

{% endblock %}



